@model IEnumerable<clinic_management.Models.PCollege>

@{
    ViewBag.Title = "Recover";
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Recover @Html.ActionLink("Back", "Admin", "Deletes", null, new { @class = "btn btn-default pull-right col-md-1" })</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>

<div class="row">
    <table class="table" id="tablePatientColleges">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.CollegeCode)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CollegeName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CollegeCode)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CollegeName)
                    </td>
                    <td>
                        @Html.ActionLink("Recover", "Recover", new { id = item.CollegeID })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<script>
    $(document).ready(function () {
        $('#tablePatientColleges').DataTable();
    });
</script>
